// labels are used in autogenerate CSS classes, e.g. modal-welcome
enum Modal {
  auth = 'auth',
  devices = 'devices',
  export = 'export',
  feedback = 'feedback',
  home = 'home',
  invites = 'invites',
  manual = 'manual',
  settings = 'settings',
  signup = 'signup',
  welcome = 'welcome',
}

export default Modal
