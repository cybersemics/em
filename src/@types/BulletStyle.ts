/** The bullet style applied to a context's children via `=children/=bullet`. `null` represents the default filled bullet (no `=bullet` attribute). */
type BulletStyle = 'None' | 'Ordered' | 'Alpha' | null

export default BulletStyle
