.env
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Blind sample draws. Kept out of the repository until a set is promoted to the corpus: a drawn set
# is only a measurement while nobody has read it, and committing one invites exactly that.
samples-blind-*.jsonl
