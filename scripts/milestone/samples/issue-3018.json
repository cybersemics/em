{
  "input": {
    "title": "Migrate `em` to `rem` css units",
    "body": "1 `rem` equals the font size of the root element (`<html>`).\n1 `em` equals the element's inherited font size (thus is sensitive to all ancestor's `fontSize`).\n\nCurrently, `em` is used to scale elements across the app according to `state.fontSize`, which is a user font size setting. This includes the size of buttons, panels, toolbar, icons, etc, not just font size. User controlled font size is critical for both user preference and low vision users, and mobile users do not have convenience of <kbd>Command/Ctrl</kbd> + <kbd>+</kbd>. \n\nThe use of `em` is made possible by [setting fontSize on the Content container](https://github.com/cybersemics/em/blob/7a1af9934377f3c5bd5bd68f73dc5d7947a80b4e/src/components/AppComponent.tsx#L197). However, `em` units are affected by the font size of all ancestors, which is usually not the intention. This really should have used `rem` all along. Regrettably, there are places where the desired font size *depends* on the inheritance feature, so this is not a trivial change.\n\nRefactor `em` to `rem` and fix any visual regressions.",
    "labels": ["refactor"]
  },
  "expected": "📐 Layout",
  "split": "test",
  "source": {
    "type": "github",
    "issue": 3018
  }
}
