{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Chrome",
      "type": "chrome",
      "request": "attach",
      "port": 9222,
      "url": "http://localhost:3000", // Change this to VITE_PORT if needed
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
